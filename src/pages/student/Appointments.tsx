import StudentLayout from '../../layouts/StudentLayout';
import StudentAppointmentsPage from '../../features/student/appointments/StudentAppointmentsPage';

export default function Appointments() {
  return (
    <StudentLayout>
      <StudentAppointmentsPage />
    </StudentLayout>
  );
}